{
  "name": "web-challenge",
  "version": "0.0.0+1",
  "description": "web-challenge",
  "private": true,
  "license": "MIT",
  "scripts": {
    "web:start": "cd web && npm run start && cd ../",
    "web:build": "cd web && npm run build && cd ../"
  }
}
